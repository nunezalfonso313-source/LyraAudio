name: lyra-kernel-test

on:
  workflow_dispatch:

jobs:
  kernel:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Test
        run: echo "Lyra workflow activo"
